# X509 extensions for an IKE/IPSec endpoint (server or client)
#
# Based on policy recommendations in RFC4945

basicConstraints = CA:FALSE
subjectKeyIdentifier = hash
authorityKeyIdentifier = keyid,issuer:always
keyUsage = digitalSignature,nonRepudiation

# Use of EKU is NOT RECOMMENDED per RFC4945 and is disabled by default.
# If you need it, you MUST include kp-id-ipsecIKE (OID for this shown below.)
# Should you also need additional EKU values, add them after the required one.
#
#extendedKeyUsage = 1.3.6.1.5.5.7.3.17

